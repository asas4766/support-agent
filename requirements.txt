
scikit-learn>=1.3.0

# Needed for the default provider (Groq) and the Ollama/OpenAI-compatible
# path — all three talk over the same OpenAI-shaped client.
openai>=1.0.0

# Only needed if AGENT_PROVIDER=anthropic
# anthropic>=0.40.0

# Only needed if EMBEDDING_BACKEND=sentence-transformers
# chromadb>=0.5.0
# sentence-transformers>=3.0.0

# Optional: for running the test suite
pytest>=7.4.0

